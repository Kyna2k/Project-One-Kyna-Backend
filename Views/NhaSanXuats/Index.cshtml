@model IEnumerable<KynaShop.Models.NhaSanXuat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenNhaSanXuat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hinh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenNhaSanXuat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hinh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaNhaSanXuat">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaNhaSanXuat">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaNhaSanXuat">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
